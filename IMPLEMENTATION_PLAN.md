# AutoVPN Bot 功能实现清单 (v1.19.18)

## ✅ 已完成

### 1. 首页重构
- ✅ 集群统计显示（在线/离线/总计）
- ✅ 节点状态卡片（🟢🟡🔴⚫）
- ✅ 新的按钮布局（7 个核心功能）
- ✅ 删除"路由管理"
- ✅ 集群令牌移到主菜单

### 2. 数据库表结构
- ✅ traffic_stats 表（小时/天/月维度）

---

## ⏳ 待实现

### 3. 集群令牌页面 (`show_cluster_token`)
**功能**：
- 显示 CLUSTER_TOKEN
- 显示 CF_WORKER_URL
- 一键复制部署命令
- 重新生成令牌

**回调函数**：
```javascript
if (cbData === "show_cluster_token") {
    // 显示令牌和配置信息
}
```

---

### 4. 节点详情页面 (`node_detail_{node_id}`)
**功能**：
- 节点详细配置（UUID、端口、域名等）
- 节点操作菜单（重启、删除、重命名）
- 节点日志查看
- 流量详情

**回调函数**：
```javascript
if (cbData?.startsWith("node_detail_")) {
    // 显示节点详情
}
```

---

### 5. 数据统计页面 (`show_traffic_stats`)
**功能**：
- 小时维度统计
- 天维度统计
- 月维度统计
- 流量排行

**回调函数**：
```javascript
if (cbData === "show_traffic_stats") {
    // 显示统计数据
}
```

---

### 6. 节点管理页面 (`node_management`)
**功能**：
- 节点列表（带复选框）
- 批量重启
- 批量删除
- 批量刷新

**回调函数**：
```javascript
if (cbData === "node_management") {
    // 显示管理界面
}
```

---

### 7. 告警逻辑优化
**当前逻辑**：
- 节点离线时发送告警
- 节点恢复时发送通知

**需要优化**：
- 超过 3 次未更新状态立即报告
- 恢复时也立即报告
- 告警频率控制

**实现位置**：
- `/report` 接口
- `scheduled` 定时任务（每分钟检查）

---

## 📋 实现优先级

### 高优先级（今天完成）
1. ✅ 首页重构
2. ⏳ 集群令牌页面
3. ⏳ 节点详情页面
4. ⏳ 数据统计页面

### 中优先级（明天完成）
5. ⏳ 节点管理页面
6. ⏳ 批量操作
7. ⏳ 告警逻辑优化

### 低优先级（后续）
- 流量趋势图表
- 更多统计维度

---

## 🎯 下一步

现在首页已完成，接下来实现：
1. 集群令牌页面
2. 节点详情页面
3. 数据统计页面

每个页面都需要：
- 回调函数处理
- UI 显示逻辑
- 数据库查询
